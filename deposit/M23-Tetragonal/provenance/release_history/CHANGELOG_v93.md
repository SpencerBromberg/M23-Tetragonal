# v93 changelog - 26 August 2026

- Replaced the open 168-entry q-pair reconstruction target with a completed parity-paired good-frame reconstruction at slots 231 and 469.
- Added exact `q^18` carry cancellation and primitive integer adjoint numerators `H1,H2`.
- Added the 42-prime, 120-coefficient arithmetic-frame reconstruction and held-out checks.
- Added the exact characteristic-zero global identity theorem using 971 exact specializations and weighted-degree bound 970.
- Tightened the proof that `z` is not in `Q(t)` using the nonzero specialization minor and `deg_v H_i < deg_v F`.
- Distinguished the symmetric coordinate `w` from the arithmetic coordinate `z` throughout.
- Reclassified previous symmetric-frame LLL/CRT failures as retained diagnostics.
- Kept degree-980 source activation as an explicit separate source-data boundary.
- Rebuilt top-level reviewer instructions, execution wrappers, metadata, and package inventory for peer review and Zenodo deposit.
