# v105 final submission filing increment

- Made the abstract self-contained by identifying the 22 tables as 21 reconstruction primes plus one independent held-out prime and replacing an undefined abstract-only acronym with direct attribution.
- Defined the monic model before using `G_u` and distinguished local orders from the published coordinate `v`.
- Added and passed a nine-part out-of-window adversarial regression at three large fresh primes.
- Synchronized all post-mint DOI metadata and corrected stale pre-mint audit language.
- Recompiled, rerendered, reran the proof-facing components, regenerated manifests, and repeated clean-room package verification.
- Updated the exact-suite runner to use the equivalent parallel all-range evaluator; the historical segmented verifier remains available.
- Ran the two independent exact-suite checks concurrently to avoid cumulative CPU throttling while preserving separate outputs and failure propagation.
- Made the legacy q-pair diagnostic checksum relocation-safe and labeled the superseded next-run note as historical.
- Cleaned the historical patch bundle of embedded bytecode and malformed empty paths, regenerated internal checksums, and labeled provisional claims as superseded.
